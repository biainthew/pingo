spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pingo?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: bia
    password: bia1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

server:
  port: 8000

springdoc:
  swagger-ui:
    path: /swagger-ui

jwt:
  secret: pingosecretkey12345678901234567890
  access-token-expiration: 1800000      # 30분
  refresh-token-expiration: 604800000   # 7일

logging:
  level:
    aib.pingo: DEBUG